services:
  - type: web
    name: expense-tracker-backend
    env: node
    buildCommand: npm install
    startCommand: node src/backend/server.js
    envVars:
      - key: MONGO_URI
        sync: false
      - key: PORT
        value: 5000
    autoDeploy: true